[build]
  builder = "DOCKERFILE"
  dockerfilePath = "Dockerfile.realtime"

[deploy]
  startCommand = "sh -c 'uvicorn src.realtime.server:app --host 0.0.0.0 --port ${PORT:-8001}'"
  healthcheckPath = "/health"
  restartPolicyType = "ON_FAILURE"

[service]
  port = 8001
