[build]
  builder = "DOCKERFILE"
  dockerfilePath = "Dockerfile.realtime"

[deploy]
  startCommand = "uvicorn src.realtime.server:app --host 0.0.0.0 --port $PORT"
  healthcheckPath = "/health"
  restartPolicyType = "ON_FAILURE"

[service]
  port = 8001
