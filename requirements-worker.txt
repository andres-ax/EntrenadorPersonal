# =============================================================================
# EntrenadorAX - Dependencias del servicio worker (arq)
# =============================================================================
# Solo se instala en el servicio Railway `worker`, NO en `bot-api`.
# Razon: arq todavia requiere redis<5 mientras que openai-agents requiere
# redis>=7. Aislamos arq en su propio service para evitar el conflicto.
#
# Instalacion:
#   pip install -r requirements.txt -r requirements-worker.txt
#
# Cuando arq publique una version compatible con redis>=7, esto se puede
# consolidar de vuelta a requirements.txt.
# =============================================================================

# Worker liviano para sync de wearables, recalcular rankings, etc.
# Pin a versiones que aceptan redis 4.x; el resto del bot usa redis>=7.
arq>=0.26,<1.0
redis>=4.2,<5.0
